name: ar_blumen_garten

description: Eine Augmented-Reality-Anwendung zur Visualisierung von Blumen in einem Garten.

version: 1.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Weitere Abhängigkeiten für AR können hier hinzugefügt werden.
  # Fügen Sie Pakete hinzu, die für die funktionale Entwicklung erforderlich sind. 
  # Beispiel für AR-Pakete:
  # arcore_flutter_plugin: ^0.7.0
  # ar_flutter_plugin: ^0.5.0

  # Beispielabhängigkeiten für die AR-Funktionalität hinzufügen:
  arcore_flutter_plugin: ^0.7.0
  ar_flutter_plugin: ^0.5.0